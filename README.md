# Relay Server

Relay is a messaging application designed to be secure, annonymous and completely free (as in freedom!)

For now this will serve as placeholder documentation and to track small tasks

## Running

To setup dependencies run `go mod tidy`, then to run the server run `go run .`

## Structure

TODO!

# Todo

- Write documentation
- Move separate *.go files into their own modules
- Move to using the `log` package instead of `fmt.Println`
- Write startup functions for setting up user data paths if desired








